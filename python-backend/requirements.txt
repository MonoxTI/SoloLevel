# Web framework
fastapi>=0.111
uvicorn[standard]>=0.30

# Database — async SQLAlchemy + PostgreSQL driver
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29

# Config / validation
pydantic>=2.0
pydantic-settings>=2.0

# HTTP client (internal API calls between services)
httpx>=0.27

# Task scheduling (trading + ML + miss-penalty jobs)
apscheduler>=3.10

# Market data
yfinance>=0.2

# Data / ML
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4

# MetaTrader 5 — Windows only, comment out on Linux/Mac
# MetaTrader5>=5.0